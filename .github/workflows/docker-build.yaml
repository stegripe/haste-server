name: Docker Build

on:
  release:
    types:
      - created
  push:
    branches:
      - "**"
    paths:
      - "Dockerfile"
  pull_request:
    branches:
      - "**"
    paths:
      - "Dockerfile"
  workflow_dispatch:

jobs:
  docker:
    uses: stegripe/workflows/.github/workflows/docker-build-and-push.yaml@main # main

